# Keep control flow in deterministic code

The TypeScript coordinator owns traversal, work allocation, option materialization, validation, retries, persistence, termination, and reduction; agents only return bounded structured judgment. Agent-directed orchestration and direct chat were rejected as the default because they make coverage, replay, cost, and failure behavior difficult to verify, while Sandcastle and other runtimes can still plug in behind the agent-runner boundary.
